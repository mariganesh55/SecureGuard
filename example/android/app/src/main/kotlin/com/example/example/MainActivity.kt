package com.example.example

import io.flutter.embedding.android.FlutterActivity

class MainActivity : FlutterActivity() {
    // That's it! Native security is automatic.
    // No channels, no callbacks, no configuration needed.
}

